# ---------------------------------------------------------------------------
# Construction Safety Monitor — AI Backend
# VERIFIED working set: Windows 11 + Python 3.11 + NVIDIA GPU (CUDA 12.1)
#
# These are the versions actually present in the developer's working venv.
# Use this file instead of requirements.txt (which is stale — it pins
# torch 2.8.0+cu121, a build that was never published, and ultralytics
# 8.0.220, which predates YOLO11 and cannot load the yolo11*.pt weights).
#
# INSTALL ORDER MATTERS.
#
#   py -3.11 -m venv venv
#   venv\Scripts\activate
#   python -m pip install --upgrade pip
#
#   1) GPU PyTorch first, from the PyTorch index:
#      pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
#      RTX 50-series (Blackwell) instead needs CUDA 12.8:
#      pip install torch==2.7.1+cu128 torchvision==0.22.1+cu128 torchaudio==2.7.1+cu128 --index-url https://download.pytorch.org/whl/cu128
#
#   2) Then everything below:
#      pip install -r requirements-gpu.txt
#
#   3) Verify the GPU is actually being used:
#      python -c "import torch;print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# ---------------------------------------------------------------------------

# --- Web framework ---
fastapi==0.104.1
uvicorn[standard]==0.24.0
starlette==0.27.0
python-multipart==0.0.6
aiofiles==23.2.1
websockets==15.0.1

# --- Config / validation ---
pydantic==2.12.0
pydantic-settings==2.11.0
email-validator==2.3.0
python-dotenv==1.1.1

# --- Computer vision ---
# numpy MUST stay <2.0 — mediapipe 0.10.8 is built against the 1.x ABI.
numpy==1.24.3
opencv-python==4.8.1.78
opencv-contrib-python==4.11.0.86
Pillow==10.1.0
mediapipe==0.10.8
# protobuf MUST stay <4 for mediapipe 0.10.8.
protobuf==3.20.3
# ultralytics >=8.3 is required to load YOLO11 checkpoints.
ultralytics==8.3.209
# lap powers the BoT-SORT tracker used for worker track IDs.
lap==0.5.13

# --- Database ---
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# --- Auth (JWT verification only; tokens are issued by the auth server) ---
python-jose[cryptography]==3.3.0
cryptography==41.0.7
